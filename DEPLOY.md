# دیپلوی روی هم‌روش (یا هر PaaS مبتنی بر Docker)

اپ یک کانتینر است که سایت معرفی، اپ و API را با هم سرو می‌کند. دیتابیس SQLite است و باید روی یک دیسک ماندگار باشد.

## ۱. ساخت اپ

در پنل هم‌روش یک اپ جدید بسازید و منبع را **مخزن گیت با Dockerfile** انتخاب کنید (برنچ: همین برنچ یا `main`). `Dockerfile` در ریشه‌ی پروژه است.

آرگومان‌های build:

| آرگومان | مقدار |
|---|---|
| `SITE_URL` | آدرس نهایی سایت، مثلاً `https://digigharz.ir` (برای canonical و sitemap) |
| `NODE_IMAGE` | فقط اگر build در دریافت `node:22-alpine` از Docker Hub خطا داد: آدرس همین ایمیج در mirror هم‌روش |
| `NPM_REGISTRY` | فقط اگر `npm ci` به npmjs دسترسی نداشت: آدرس یک mirror از npm |

## ۲. تنظیمات اجرا

- **پورت:** `3000`
- **تعداد replica:** فقط **۱** (SQLite و زمان‌بندی قرعه‌ها روی یک نمونه اجرا می‌شوند)
- **دیسک ماندگار:** یک دیسک (مثلاً ۱ گیگ) روی مسیر `/app/data` مونت کنید. بدون آن، با هر دیپلوی همه‌ی اطلاعات پاک می‌شود.
- **Health check:** مسیر `/api/health`
- **دامنه:** دامنه را وصل و **HTTPS** را فعال کنید. کوکی ورود فقط روی HTTPS کار می‌کند.
- **منابع:** برای شروع ۰٫۲۵ CPU و ۲۵۶ مگابایت رم کافی است.

## ۳. متغیرهای محیطی

| متغیر | نسخه‌ی نمایشی (الان) | نسخه‌ی واقعی (بعد از SDK دیجی‌پی) |
|---|---|---|
| `DEMO_MODE` | `true` | خالی |
| `DIGIPAY_MODE` | خالی (شبیه‌ساز) | `live` |
| `SMSIR_API_KEY` | کلید API از پنل SMS.ir (اختیاری) | لازم برای ورود با پیامک |
| `SMSIR_TEMPLATE_ID` | شناسه‌ی قالب «ارسال کد» تأییدشده در SMS.ir | لازم |
| `SMSIR_TEMPLATE_PARAM` | اسم پارامتر قالب؛ پیش‌فرض `CODE` | همان |
| `SMSIR_SANDBOX` | `true` اگر کلید از نوع Sandbox است | خالی |
| `OPS_PHONES` | اختیاری | شماره‌های مجاز به پنل عملیات |
| `CIRCLE_FORM_TIMEOUT_MIN` | مثلاً `60` | در سطح بلوغ `5` |

**کلید Sandbox در SMS.ir:** با کلید Sandbox هیچ پیامکی واقعاً ارسال نمی‌شود. `SMSIR_SANDBOX=true` بگذارید و `SMSIR_TEMPLATE_ID` را خالی بگذارید تا قالب پیش‌فرض Sandbox (`123456`) استفاده شود. در این حالت درخواست واقعاً به SMS.ir می‌رود (پس درستی کلید و اتصال بررسی می‌شود) ولی کد ورود روی صفحه هم نمایش داده می‌شود. سرور در production فقط همراه با `DEMO_MODE=true` این حالت را قبول می‌کند.

**قالب پیامک در SMS.ir:** در پنل، بخش ارسال سریع (Verify)، یک قالب با متنی مثل `کد ورود شما به دیجی قرض: #CODE#` بسازید و بعد از تأیید، شناسه‌اش را در `SMSIR_TEMPLATE_ID` بگذارید. اگر اسم پارامتر را چیز دیگری گذاشتید، همان را در `SMSIR_TEMPLATE_PARAM` بنویسید. وقتی کلید پیامک تنظیم باشد، کد ورود دیگر روی صفحه نمایش داده نمی‌شود و واقعاً پیامک می‌شود (حتی در حالت نمایشی).

> کلید API را فقط در بخش متغیرهای محیطی هم‌روش یا فایل `.env` بگذارید؛ هیچ‌وقت داخل کد یا گیت نه.

`NODE_ENV`، `PORT` و `DB_PATH` در Dockerfile تنظیم شده‌اند و لازم نیست دستشان بزنید.

> **هشدار:** در `DEMO_MODE=true` هر کسی با لینک `/?dp_token=sim-<شماره>` با هر شماره‌ای وارد می‌شود، کد ورود روی صفحه نشان داده می‌شود و پنل عملیات برای همه باز است. فقط برای نمایش استفاده کنید، نه برای کاربر واقعی. سرور در حالت production بدون `DEMO_MODE=true` و بدون `DIGIPAY_MODE=live` اصلاً بالا نمی‌آید تا این اشتباهی رخ ندهد.

## ۴. بعد از دیپلوی

- `https://<دامنه>/api/health` باید `{"ok":true,...}` برگرداند.
- ورود آزمایشی: `https://<دامنه>/?dp_token=sim-09121234567`
- پشتیبان: فایل `/app/data/digi-gharz.db` (همراه با `-wal` و `-shm`) را به‌طور منظم کپی کنید.

## اجرای همین کانتینر روی سیستم خودتان

```bash
docker build --build-arg SITE_URL=http://localhost:3000 -t digi-gharz .
docker run -p 3000:3000 -e DEMO_MODE=true -v digi-gharz-data:/app/data digi-gharz
```
(روی `http://localhost` کوکی Secure ذخیره نمی‌شود؛ برای تست محلی `npm run dev` راحت‌تر است.)
